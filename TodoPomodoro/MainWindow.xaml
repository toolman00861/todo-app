<Window x:Class="TodoPomodoro.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TodoPomodoro"
        xmlns:viewmodels="clr-namespace:TodoPomodoro.ViewModels"
        mc:Ignorable="d"
        Title="待办事项 + 番茄钟" Height="700" Width="1100"
        MinHeight="600" MinWidth="900"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStyle="None"
        ResizeMode="CanResizeWithGrip">
    
    <Window.Resources>
        <!-- 转换器 -->
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        
        <!-- 阴影效果 -->
        <DropShadowEffect x:Key="StandardShadow" 
                          Direction="270" 
                          BlurRadius="10" 
                          ShadowDepth="3" 
                          Opacity="0.3" 
                          Color="#000000"/>
        
        <!-- 圆角按钮样式 -->
        <Style x:Key="RoundButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border x:Name="ShadowBorder" 
                                    Background="Transparent" 
                                    CornerRadius="20" 
                                    Margin="0,0,0,0">
                                <Border.Effect>
                                    <DropShadowEffect Direction="270" 
                                                      BlurRadius="5" 
                                                      ShadowDepth="2" 
                                                      Opacity="0.3" 
                                                      Color="#000000"/>
                                </Border.Effect>
                            </Border>
                            <Border x:Name="MainBorder" 
                                    Background="{TemplateBinding Background}" 
                                    CornerRadius="20" 
                                    BorderThickness="0">
                                <ContentPresenter HorizontalAlignment="Center" 
                                                  VerticalAlignment="Center"/>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#66BB6A" TargetName="MainBorder"/>
                                <Setter Property="RenderTransform" TargetName="MainBorder">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="1.05" ScaleY="1.05"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="RenderTransformOrigin" TargetName="MainBorder" Value="0.5,0.5"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#388E3C" TargetName="MainBorder"/>
                                <Setter Property="RenderTransform" TargetName="MainBorder">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.95" ScaleY="0.95"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#CCCCCC" TargetName="MainBorder"/>
                                <Setter Property="Opacity" Value="0.7" TargetName="MainBorder"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 番茄钟按钮样式 -->
        <Style x:Key="PomodoroButtonStyle" TargetType="Button" BasedOn="{StaticResource RoundButtonStyle}">
            <Setter Property="Width" Value="110"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        
        <!-- 待办事项样式 -->
        <Style x:Key="TodoItemStyle" TargetType="ListBoxItem">
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Margin" Value="0,4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Grid>
                            <Border x:Name="ShadowBorder" 
                                    Background="Transparent" 
                                    CornerRadius="8" 
                                    Margin="0,0,0,0">
                                <Border.Effect>
                                    <DropShadowEffect Direction="270" 
                                                      BlurRadius="4" 
                                                      ShadowDepth="1" 
                                                      Opacity="0.2" 
                                                      Color="#000000"/>
                                </Border.Effect>
                            </Border>
                            <Border x:Name="MainBorder" 
                                    Background="{TemplateBinding Background}" 
                                    CornerRadius="8" 
                                    BorderThickness="0">
                                <ContentPresenter Margin="{TemplateBinding Padding}"/>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#E3F2FD" TargetName="MainBorder"/>
                                <Setter Property="BorderThickness" Value="0" TargetName="MainBorder"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F5F5F5" TargetName="MainBorder"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Border CornerRadius="15" Background="White" Margin="10" Effect="{StaticResource StandardShadow}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
            
            <!-- 标题栏 -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- 应用标题 -->
                <Border Grid.Column="0" Background="#2196F3" CornerRadius="15,15,0,0" Padding="20,15">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="{StaticResource AppIcon}" Width="32" Height="32" Margin="0,0,10,0"/>
                        <TextBlock Text="待办事项 + 番茄钟" 
                                   FontSize="22" 
                                   FontWeight="Bold" 
                                   Foreground="White"
                                   VerticalAlignment="Center"/>
                        
                        <!-- 设置按钮 -->
                        <Button Command="{Binding SettingsViewModel.ShowSettingsCommand}"
                                Background="Transparent"
                                BorderThickness="0"
                                Margin="20,0,0,0"
                                Cursor="Hand"
                                ToolTip="设置">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" 
                                                        CornerRadius="8"
                                                        Padding="8">
                                                    <Path Fill="White" 
                                                          Width="20" 
                                                          Height="20"
                                                          Stretch="Uniform"
                                                          Data="M12,15.5A3.5,3.5 0 0,1 8.5,12A3.5,3.5 0 0,1 12,8.5A3.5,3.5 0 0,1 15.5,12A3.5,3.5 0 0,1 12,15.5M19.43,12.97C19.47,12.65 19.5,12.33 19.5,12C19.5,11.67 19.47,11.34 19.43,11L21.54,9.37C21.73,9.22 21.78,8.95 21.66,8.73L19.66,5.27C19.54,5.05 19.27,4.96 19.05,5.05L16.56,6.05C16.04,5.66 15.5,5.32 14.87,5.07L14.5,2.42C14.46,2.18 14.25,2 14,2H10C9.75,2 9.54,2.18 9.5,2.42L9.13,5.07C8.5,5.32 7.96,5.66 7.44,6.05L4.95,5.05C4.73,4.96 4.46,5.05 4.34,5.27L2.34,8.73C2.22,8.95 2.27,9.22 2.46,9.37L4.57,11C4.53,11.34 4.5,11.67 4.5,12C4.5,12.33 4.53,12.65 4.57,12.97L2.46,14.63C2.27,14.78 2.22,15.05 2.34,15.27L4.34,18.73C4.46,18.95 4.73,19.03 4.95,18.95L7.44,17.94C7.96,18.34 8.5,18.68 9.13,18.93L9.5,21.58C9.54,21.82 9.75,22 10,22H14C14.25,22 14.46,21.82 14.5,21.58L14.87,18.93C15.5,18.68 16.04,18.34 16.56,17.94L19.05,18.95C19.27,19.03 19.54,18.95 19.66,18.73L21.66,15.27C21.78,15.05 21.73,14.78 21.54,14.63L19.43,12.97Z"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#1976D2"/>
                                                    </Trigger>
                                                    <Trigger Property="IsPressed" Value="True">
                                                        <Setter Property="Background" Value="#1565C0"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>
                </Border>
                
                <!-- 窗口控制按钮 -->
                <Border Grid.Column="1" Background="#2196F3" CornerRadius="0,15,0,0">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button x:Name="MinimizeButton" 
                                Content="—" 
                                Width="46" 
                                Height="46" 
                                Background="Transparent" 
                                BorderThickness="0" 
                                Foreground="White" 
                                FontWeight="Bold" 
                                Click="MinimizeButton_Click">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" 
                                                        CornerRadius="0">
                                                    <ContentPresenter HorizontalAlignment="Center" 
                                                                      VerticalAlignment="Center"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#1976D2"/>
                                                    </Trigger>
                                                    <Trigger Property="IsPressed" Value="True">
                                                        <Setter Property="Background" Value="#1565C0"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>
                        
                        <Button x:Name="CloseButton" 
                                Content="✕" 
                                Width="46" 
                                Height="46" 
                                Background="Transparent" 
                                BorderThickness="0" 
                                Foreground="White" 
                                FontWeight="Bold" 
                                Click="CloseButton_Click">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" 
                                                        CornerRadius="0,15,0,0">
                                                    <ContentPresenter HorizontalAlignment="Center" 
                                                                      VerticalAlignment="Center"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#F44336"/>
                                                    </Trigger>
                                                    <Trigger Property="IsPressed" Value="True">
                                                        <Setter Property="Background" Value="#D32F2F"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>
                </Border>
            </Grid>
            
            <!-- 主内容区 -->
            <Grid Grid.Row="1" Margin="20,15,20,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" MinWidth="400"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="1.5*" MinWidth="350"/>
                </Grid.ColumnDefinitions>
                
                <!-- 待办事项区域 -->
                <Border Grid.Column="0" 
                        Background="#FAFAFA" 
                        CornerRadius="12" 
                        Effect="{StaticResource StandardShadow}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <!-- 待办事项标题 -->
                        <Border Background="#E3F2FD" 
                                CornerRadius="12,12,0,0" 
                                Padding="18,15" 
                                BorderThickness="0">
                            <StackPanel Orientation="Horizontal">
                                <Image Source="{StaticResource TaskIcon}" Width="24" Height="24" Margin="0,0,12,0"/>
                                <TextBlock Text="待办事项" 
                                           FontSize="20" 
                                           FontWeight="SemiBold" 
                                           Foreground="#1976D2"
                                           VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        
                        <!-- 待办事项列表 -->
                        <ListBox x:Name="TodoListBox"
                                 Grid.Row="1" 
                                 ItemsSource="{Binding TodoViewModel.TodoItems}" 
                                 SelectedItem="{Binding TodoViewModel.SelectedTodoItem}" 
                                 ItemContainerStyle="{StaticResource TodoItemStyle}"
                                 BorderThickness="0"
                                 Background="Transparent"
                                 Margin="18,15,18,0"
                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                 ScrollViewer.VerticalScrollBarVisibility="Auto">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,6">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <!-- 完成状态复选框 -->
                                        <CheckBox Grid.Column="0" 
                                                  IsChecked="{Binding IsCompleted}" 
                                                  VerticalAlignment="Center" 
                                                  Margin="0,0,12,0">
                                            <CheckBox.Template>
                                                <ControlTemplate TargetType="CheckBox">
                                                    <Grid>
                                                        <Border x:Name="Border" 
                                                                Width="24" 
                                                                Height="24" 
                                                                CornerRadius="12" 
                                                                Background="White" 
                                                                BorderThickness="2" 
                                                                BorderBrush="#BBDEFB">
                                                            <Path x:Name="Checkmark" 
                                                                  Stroke="White" 
                                                                  StrokeThickness="2" 
                                                                  Data="M 6,12 L 10,16 L 18,8" 
                                                                  Visibility="Collapsed"/>
                                                        </Border>
                                                    </Grid>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsChecked" Value="True">
                                                            <Setter Property="Background" Value="#4CAF50" TargetName="Border"/>
                                                            <Setter Property="BorderBrush" Value="#4CAF50" TargetName="Border"/>
                                                            <Setter Property="Visibility" Value="Visible" TargetName="Checkmark"/>
                                                        </Trigger>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="BorderBrush" Value="#2196F3" TargetName="Border"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </CheckBox.Template>
                                        </CheckBox>
                                        
                                        <!-- 待办事项内容 -->
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Text="{Binding Title}" 
                                                       FontWeight="SemiBold" 
                                                       FontSize="15"
                                                       TextWrapping="Wrap"
                                                       LineHeight="20">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                                                <Setter Property="TextDecorations" Value="Strikethrough"/>
                                                                <Setter Property="Foreground" Value="#9E9E9E"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                            
                                            <TextBlock Text="{Binding Description}" 
                                                       Foreground="#757575" 
                                                       FontSize="13"
                                                       TextWrapping="Wrap" 
                                                       LineHeight="18"
                                                       Margin="0,6,0,0"
                                                       Visibility="{Binding Description, Converter={StaticResource StringToVisibilityConverter}}"/>
                                            
                                            <StackPanel Orientation="Horizontal" 
                                                        Margin="0,10,0,0">
                                                <Border Background="#F5F5F5" 
                                                        CornerRadius="4" 
                                                        Padding="8,3">
                                                    <TextBlock Text="{Binding CreatedAt, StringFormat='{}{0:yyyy-MM-dd}'}" 
                                                               Foreground="#757575" 
                                                               FontSize="12"/>
                                                </Border>
                                                
                                                <Border Background="{Binding Priority, Converter={StaticResource PriorityToColorConverter}}" 
                                                        CornerRadius="4" 
                                                        Padding="8,3" 
                                                        Margin="8,0,0,0">
                                                    <TextBlock Text="{Binding Priority}" 
                                                               Foreground="White" 
                                                               FontSize="12"
                                                               FontWeight="Medium"/>
                                                </Border>
                                                
                                                <Border Background="#FFEBEE" 
                                                        CornerRadius="4" 
                                                        Padding="8,3" 
                                                        Margin="8,0,0,0">
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="🍅" 
                                                                   FontSize="12" 
                                                                   Margin="0,0,4,0"/>
                                                        <TextBlock Text="{Binding CompletedPomodoros}" 
                                                                   Foreground="#D32F2F" 
                                                                   FontSize="12"
                                                                   FontWeight="Medium"/>
                                                        <TextBlock Text="/" 
                                                                   Foreground="#9E9E9E" 
                                                                   FontSize="12"/>
                                                        <TextBlock Text="{Binding EstimatedPomodoros}" 
                                                                   Foreground="#9E9E9E" 
                                                                   FontSize="12"/>
                                                    </StackPanel>
                                                </Border>
                                            </StackPanel>
                                        </StackPanel>
                                        
                                        <!-- 优先级指示器 -->
                                        <Border Grid.Column="2" 
                                                Width="6" 
                                                Height="Auto"
                                                Background="{Binding Priority, Converter={StaticResource PriorityToColorConverter}}" 
                                                HorizontalAlignment="Right" 
                                                CornerRadius="3"
                                                Margin="12,0,0,0"/>
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                        
                        <!-- 添加待办事项 -->
                        <Grid Grid.Row="2" Margin="18,15,18,18">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <Border Grid.Column="0" 
                                    Background="White" 
                                    CornerRadius="25" 
                                    BorderThickness="2" 
                                    BorderBrush="#E0E0E0"
                                    Margin="0,0,15,0">
                                <TextBox Text="{Binding TodoViewModel.NewTodoTitle, UpdateSourceTrigger=PropertyChanged}" 
                                         Padding="20,14" 
                                         BorderThickness="0"
                                         Background="Transparent"
                                         FontSize="14"
                                         VerticalAlignment="Center"/>
                            </Border>
                            
                            <Button Grid.Column="1" 
                                    Content="添加" 
                                    Command="{Binding TodoViewModel.AddTodoCommand}" 
                                    Style="{StaticResource RoundButtonStyle}" 
                                    MinWidth="85"
                                    Height="46"/>
                        </Grid>
                    </Grid>
                </Border>
                
                <!-- 分隔线 -->
                <GridSplitter Grid.Column="1" 
                              Width="1" 
                              HorizontalAlignment="Center" 
                              VerticalAlignment="Stretch" 
                              Background="#E0E0E0"
                              Margin="15,0"/>
                
                <!-- 番茄钟区域 -->
                <Border Grid.Column="2" 
                        Background="#FAFAFA" 
                        CornerRadius="12" 
                        Effect="{StaticResource StandardShadow}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <!-- 番茄钟标题 -->
                        <Border Background="#FFEBEE" 
                                CornerRadius="12,12,0,0" 
                                Padding="18,15" 
                                BorderThickness="0">
                            <StackPanel Orientation="Horizontal">
                                <Image Source="{StaticResource PomodoroIcon}" Width="24" Height="24" Margin="0,0,12,0"/>
                                <TextBlock Text="番茄钟" 
                                           FontSize="20" 
                                           FontWeight="SemiBold" 
                                           Foreground="#D32F2F"
                                           VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        
                        <!-- 番茄钟内容 -->
                        <Grid x:Name="PomodoroPanel" Grid.Row="1" Margin="25,20,25,25">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <!-- 当前状态 -->
                            <Border Grid.Row="0" 
                                    Background="#F5F5F5" 
                                    CornerRadius="22" 
                                    Padding="24,12" 
                                    HorizontalAlignment="Center" 
                                    Margin="0,0,0,30">
                                <TextBlock Text="{Binding PomodoroViewModel.StateDisplay}" 
                                           FontSize="16" 
                                           FontWeight="SemiBold" 
                                           Foreground="#424242"/>
                            </Border>
                            
                            <!-- 计时器显示 -->
                            <Grid Grid.Row="1" Margin="0,15">
                                <Ellipse Width="240" 
                                         Height="240" 
                                         Fill="#FFEBEE" 
                                         HorizontalAlignment="Center" 
                                         VerticalAlignment="Center">
                                    <Ellipse.Effect>
                                        <DropShadowEffect Direction="270" 
                                                          BlurRadius="20" 
                                                          ShadowDepth="6" 
                                                          Opacity="0.25" 
                                                          Color="#000000"/>
                                    </Ellipse.Effect>
                                </Ellipse>
                                
                                <Ellipse Width="220" 
                                         Height="220" 
                                         Fill="White" 
                                         HorizontalAlignment="Center" 
                                         VerticalAlignment="Center"/>
                                
                                <TextBlock Text="{Binding PomodoroViewModel.TimeDisplay}" 
                                           FontSize="52" 
                                           FontWeight="Bold" 
                                           Foreground="#D32F2F" 
                                           HorizontalAlignment="Center" 
                                           VerticalAlignment="Center"/>
                            </Grid>
                            
                            <!-- 番茄钟计数 -->
                            <Border Grid.Row="2" 
                                    Background="#F5F5F5" 
                                    CornerRadius="22" 
                                    Padding="24,12" 
                                    HorizontalAlignment="Center" 
                                    Margin="0,30,0,35">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="已完成番茄：" 
                                               FontSize="15" 
                                               Foreground="#424242"
                                               VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding PomodoroViewModel.PomodoroCountDisplay}" 
                                               FontSize="19" 
                                               FontWeight="Bold" 
                                               Foreground="#D32F2F" 
                                               VerticalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                            
                            <!-- 控制按钮 -->
                            <StackPanel Grid.Row="3" 
                                        Orientation="Horizontal" 
                                        HorizontalAlignment="Center" 
                                        Margin="0,15">
                                <Button Content="开始" 
                                        Style="{StaticResource PomodoroButtonStyle}" 
                                        Command="{Binding PomodoroViewModel.StartCommand}" 
                                        Margin="0,0,18,0"
                                        MinWidth="90"
                                        Height="48"/>
                                <Button Content="暂停" 
                                        Style="{StaticResource PomodoroButtonStyle}" 
                                        Command="{Binding PomodoroViewModel.PauseCommand}" 
                                        Margin="0,0,18,0"
                                        MinWidth="90"
                                        Height="48"/>
                                <Button Content="重置" 
                                        Style="{StaticResource PomodoroButtonStyle}" 
                                        Command="{Binding PomodoroViewModel.ResetCommand}"
                                        MinWidth="90"
                                        Height="48"/>
                            </StackPanel>
                        </Grid>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Border>
    
    <!-- 设置面板覆盖层 -->
    <Grid Visibility="{Binding SettingsViewModel.IsVisible, Converter={StaticResource BooleanToVisibilityConverter}}"
          Background="#80000000"
          Panel.ZIndex="1000">
            <Border Background="White"
                    CornerRadius="15"
                    MaxWidth="500"
                    MaxHeight="600"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Effect="{StaticResource StandardShadow}">
                <Grid Margin="30">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <!-- 标题栏 -->
                    <Grid Grid.Row="0" Margin="0,0,0,20">
                        <TextBlock Text="番茄钟设置" 
                                   FontSize="24" 
                                   FontWeight="Bold" 
                                   Foreground="#2196F3"
                                   HorizontalAlignment="Left"/>
                        <Button Command="{Binding SettingsViewModel.HideSettingsCommand}"
                                Content="✕"
                                Background="Transparent"
                                BorderThickness="0"
                                FontSize="20"
                                Foreground="#666"
                                HorizontalAlignment="Right"
                                Cursor="Hand"
                                Width="30"
                                Height="30"/>
                    </Grid>
                    
                    <!-- 设置内容 -->
                    <StackPanel Grid.Row="1">
                        <!-- 工作时间设置 -->
                        <Grid Margin="0,0,0,20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="工作时间（分钟）" 
                                       FontSize="16" 
                                       VerticalAlignment="Center"/>
                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <Button Content="-" 
                                        Width="30" 
                                        Height="30"
                                        Command="{Binding SettingsViewModel.DecreaseWorkDurationCommand}"/>
                                <TextBox Text="{Binding SettingsViewModel.WorkDuration, UpdateSourceTrigger=PropertyChanged}"
                                         Width="60"
                                         Height="30"
                                         TextAlignment="Center"
                                         Margin="5,0"/>
                                <Button Content="+" 
                                        Width="30" 
                                        Height="30"
                                        Command="{Binding SettingsViewModel.IncreaseWorkDurationCommand}"/>
                            </StackPanel>
                        </Grid>
                        
                        <!-- 短休息时间设置 -->
                        <Grid Margin="0,0,0,20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="短休息时间（分钟）" 
                                       FontSize="16" 
                                       VerticalAlignment="Center"/>
                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <Button Content="-" 
                                        Width="30" 
                                        Height="30"
                                        Command="{Binding SettingsViewModel.DecreaseShortBreakCommand}"/>
                                <TextBox Text="{Binding SettingsViewModel.ShortBreakDuration, UpdateSourceTrigger=PropertyChanged}"
                                         Width="60"
                                         Height="30"
                                         TextAlignment="Center"
                                         Margin="5,0"/>
                                <Button Content="+" 
                                        Width="30" 
                                        Height="30"
                                        Command="{Binding SettingsViewModel.IncreaseShortBreakCommand}"/>
                            </StackPanel>
                        </Grid>
                        
                        <!-- 长休息时间设置 -->
                        <Grid Margin="0,0,0,20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="长休息时间（分钟）" 
                                       FontSize="16" 
                                       VerticalAlignment="Center"/>
                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <Button Content="-" 
                                        Width="30" 
                                        Height="30"
                                        Command="{Binding SettingsViewModel.DecreaseLongBreakCommand}"/>
                                <TextBox Text="{Binding SettingsViewModel.LongBreakDuration, UpdateSourceTrigger=PropertyChanged}"
                                         Width="60"
                                         Height="30"
                                         TextAlignment="Center"
                                         Margin="5,0"/>
                                <Button Content="+" 
                                        Width="30" 
                                        Height="30"
                                        Command="{Binding SettingsViewModel.IncreaseLongBreakCommand}"/>
                            </StackPanel>
                        </Grid>
                        
                        <!-- 长休息间隔设置 -->
                        <Grid Margin="0,0,0,20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="几个番茄钟后长休息" 
                                       FontSize="16" 
                                       VerticalAlignment="Center"/>
                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <Button Content="-" 
                                        Width="30" 
                                        Height="30"
                                        Command="{Binding SettingsViewModel.DecreasePomodoroCountCommand}"/>
                                <TextBox Text="{Binding SettingsViewModel.PomodorosUntilLongBreak, UpdateSourceTrigger=PropertyChanged}"
                                         Width="60"
                                         Height="30"
                                         TextAlignment="Center"
                                         Margin="5,0"/>
                                <Button Content="+" 
                                        Width="30" 
                                        Height="30"
                                        Command="{Binding SettingsViewModel.IncreasePomodoroCountCommand}"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                    
                    <!-- 底部按钮 -->
                    <StackPanel Grid.Row="2" 
                                Orientation="Horizontal" 
                                HorizontalAlignment="Center" 
                                Margin="0,20,0,0">
                        <Button Content="重置为默认" 
                                Style="{StaticResource RoundButtonStyle}"
                                Command="{Binding SettingsViewModel.ResetToDefaultCommand}"
                                Background="#FF9800"
                                Margin="0,0,10,0"/>
                        <Button Content="确定" 
                                Style="{StaticResource RoundButtonStyle}"
                                Command="{Binding SettingsViewModel.HideSettingsCommand}"
                                Background="#4CAF50"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
